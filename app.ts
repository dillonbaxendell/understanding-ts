//This sort of sets a template for person by assigning the types first
// const person: {
//     name: string;
//     age: number;
// } = {
const person = {
    name: "Dillon",
    age: 26
};

console.log(person.name);